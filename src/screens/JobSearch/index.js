import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import CustomHeader from '../../components/CustomHeader';

const JobSearch = () => {
  return (
    <View>
      <CustomHeader title={'Discovery'} />
    </View>
  );
};

export default JobSearch;

const styles = StyleSheet.create({});
